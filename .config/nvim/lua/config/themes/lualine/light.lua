local colors = {
    color1   = '#999999',
    color2   = '#f3f3f3',
}

return {
    visual = {
        a = { fg = colors.color1, bg = colors.color2 },
        b = { fg = colors.color1, bg = colors.color2 },
    },
    replace = {
        a = { fg = colors.color1, bg = colors.color2 },
        b = { fg = colors.color1, bg = colors.color2 },
    },
    inactive = {
        c = { fg = colors.color1, bg = colors.color2 },
        a = { fg = colors.color1, bg = colors.color2 },
        b = { fg = colors.color1, bg = colors.color2 },
    },
    normal = {
        c = { fg = colors.color1, bg = colors.color2 },
        a = { fg = colors.color1, bg = colors.color2 },
        b = { fg = colors.color1, bg = colors.color2 },
    },
    insert = {
        a = { fg = colors.color1, bg = colors.color2 },
        b = { fg = colors.color1, bg = colors.color2 },
    },
}
